apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: newrust-backend
  namespace: storefront-dev002
  labels:
    app.kubernetes.io/name: newrust-backend
    app.kubernetes.io/managed-by: deepagent
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: newrust-backend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
