apiVersion: v1
kind: Service
metadata:
  name: newrust-frontend
  namespace: storefront-development12
  labels:
    app.kubernetes.io/name: newrust-frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: newrust-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
