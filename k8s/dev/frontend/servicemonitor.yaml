apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: newrust-frontend
  namespace: storefront-dev002
  labels:
    app.kubernetes.io/name: newrust-frontend
    app.kubernetes.io/managed-by: deepagent
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: newrust-frontend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
